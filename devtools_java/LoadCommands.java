package devtools_java;

public enum LoadCommands {
    COINS,
    CRASHSERVER,
    INSTAKILL,
    ITEMS,
    MEMBERSHIP,
    STARS;

    public boolean  prod(){
        return this.prod();
    }
}
