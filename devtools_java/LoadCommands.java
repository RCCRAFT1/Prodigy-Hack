package devtools_java;

public enum LoadCommands {
    COINS,
    CRASHSERVER,
    INSTAKILL,
    ITEMS,
    MEMBERSHIP,
    BYPASS,
    STARS;

    public boolean  prod(){
        return this.prod();
    }
}
